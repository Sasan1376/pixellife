# راهنمای تغییرات پروژه Pixellife

## قانون مهم صفحهٔ محصول

قبل از تغییر در صفحهٔ محصول، نسخهٔ فعلی فایل‌های گیت‌هاب را بخوانید و فقط بخش موردنیاز را ویرایش کنید. فایل‌های قدیمیِ محلی را به‌صورت کامل روی فایل‌های جدید آپلود نکنید.

فایل‌های حساس:

- `views/product.html`: صفحهٔ محصول، گالری، قیمت، تخفیف، گزینه‌ها، بررسی تخصصی و رفتار موبایل
- `views/admin-products.html`: فرم ثبت و ویرایش محصول در ادمین
- `src/routes/adminApi.js`: ذخیرهٔ اطلاعات محصولات ادمین
- `src/models/Product.js`: ساختار دادهٔ محصول
- `tests/product-page-contract.test.js`: کنترل محافظ تغییرات صفحهٔ محصول

## قابلیت‌هایی که نباید حذف شوند

- گالری بزرگ محصول، جایگاه آیکن‌ها و حذف فلش‌های تغییر تصویر
- کارت خرید دسکتاپ، قیمت، تخفیف و قیمت قبلی
- ترتیب رنگ، حافظه، تعداد و ویژگی‌ها
- محدودشدن ویژگی‌های سریع به پنج مورد
- افزودن به سبد خرید با اولین لمس موبایل
- دریافت زندهٔ محصولات مشابه و بازدیدهای اخیر
- حذف نام کامل محصول از مسیر صفحه
- فیلدهای دستی بررسی تخصصی: نقاط قوت، نقاط ضعف، جمع‌بندی نهایی و امتیاز جمع‌بندی
- تنظیم ادمین برای پنهان‌کردن رنگ‌های محصول ناموجود

## بررسی قبل از تحویل

پس از هر تغییر مرتبط:

```bash
npm run test:product-page-contract
```

اگر این تست شکست خورد، فایل صفحهٔ محصول ناقص یا با نسخهٔ قدیمی جایگزین شده است و نباید دیپلوی انجام شود.

بعد از تغییر کد، وضعیت Render و آخرین لاگ دیپلوی را بررسی کنید. تغییرات گیت‌هاب به‌تنهایی به معنی انتشار موفق روی سایت نیستند.
